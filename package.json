{
  "name": "paper-tools",
  "private": true,
  "packageManager": "pnpm@10.18.3",
  "description": "論文関連ツール群のモノレポ",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "rimraf": "^6.1.2"
  }
}
